byta till hierachical enl egen md

ändra upsert till att använda både filename och source

fixa så att det går att köra pop o run fr root dir

RAG

	byta till bge-m3:latest som embeddings och köra om populate

	förstå och rensa o refaktorisera
		flytta ut i nytt repo och plocka bort det jag inte använder
		förstå allt och sen refaktorisera utan genererande ai-hjälp, bara fråga på specifika frågor, men helst bara läsa docs istället
			
	automatiserade tester

	optimera
		run
		populate

	få in som egna tabeller i embeddings
		icbt
		mct

	
	se om jag kan köra den som app i min telefon
		går det att splitta ut så att flera appar använder samma chat-modell?
		använda termux?

	gör klart kursen


byt nltk.sent_tokenize till LangChain (RecursiveCharacterTextSplitter) - nu är chunks alltid exakt en mening vilket blir för stolpigt 
ex:
from langchain_text_splitters import RecursiveCharacterTextSplitter
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,       # Max antal TECKEN per chunk (ca 150-200 ord)
    chunk_overlap=200,     # Hur många tecken som ska överlappa mellan två chunks
    separators=["\n\n", "\n", " ", ""] # Dela i första hand vid stycken, inte meningar
)
chunks = text_splitter.split_text(din_stora_text)
